#version 330 core

in vec3 fragmentColor;
in vec2 obj_coord;

out vec4 color;

void main() {
    vec2 uv = obj_coord*2;
    float distance = length(uv);

    color.rgb = fragmentColor;
    color.a = 1 - smoothstep(0.975, 1.0, distance);
}
